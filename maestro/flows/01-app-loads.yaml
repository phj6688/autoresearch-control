# Flow 01: App loads — core UI landmarks render
# Verifies the initial render: branding, tabs, stats bar, sidebar, empty state
url: http://localhost:3200/proxy/autoresearch
---
- launchApp

# ── HEADER / BRANDING ──
- assertVisible: "AUTORESEARCH"
- assertVisible: "MISSION CONTROL"
- assertVisible: "New Session"

# ── TAB NAVIGATION ──
- assertVisible: "Sessions"
- assertVisible: "Analytics"
- assertVisible: "Events"
- assertVisible: "Compare"

# ── STATS BAR ──
- assertVisible: "SESSIONS"
- assertVisible: "EXPERIMENTS"
- assertVisible: "COMMIT RATE"

# ── SIDEBAR ──
- assertVisible: "docboost-f1"
- assertVisible: "running"

# All core UI landmarks verified
